# This script should reboot a device registered to your Org
# Check Meraki API Dashboard Docs for Resource Path
# https://developer.cisco.com/meraki/api-latest/

import json
import meraki
import requests
import tokens

import tokens
import requests
import json


# Define your variables
arg1 = None
arg_list = []


# Create a function to reboot a single device using the request module
# The function should take an argument
def reboot_device(arg1):
   None



# Create a function to reboot a list of devices using the meraki sdk
# The function should take a list as the argument
def reboot_devices_in_list(arg_list):
    None


# Call the reboot functions
